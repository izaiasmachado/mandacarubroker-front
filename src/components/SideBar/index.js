import * as S from "./styles";

import Balance from "../BalanceGroup";

const SideBar = () => (
    <S.SideBar>
        <Balance>            
        </Balance>
    </S.SideBar>
);

export default SideBar;
